╔════════════════════════════════════════════════════════════════╗
║  🔧 FIX RECURSION ERROR - QUICK CHECKLIST                     ║
╚════════════════════════════════════════════════════════════════╝

📋 FOLLOW THESE STEPS EXACTLY:

┌────────────────────────────────────────────────────────────────┐
│ STEP 1: Open Supabase SQL Editor                              │
└────────────────────────────────────────────────────────────────┘
   
   1. Go to: https://app.supabase.com/project/mioxecluvalizougrstz/sql
   2. Click "New Query"
   
   ✅ Done? Continue to Step 2


┌────────────────────────────────────────────────────────────────┐
│ STEP 2: Run the Fix Script                                    │
└────────────────────────────────────────────────────────────────┘
   
   1. Open file: supabase-final-fix.sql
   2. Copy ALL code (Ctrl+A, Ctrl+C)
   3. Paste into SQL Editor (Ctrl+V)
   4. Click "Run" (or Ctrl+Enter)
   5. Wait for: "✅ SPINERGY RLS FIXED - NO RECURSION!"
   
   ✅ Done? Continue to Step 3


┌────────────────────────────────────────────────────────────────┐
│ STEP 3: Disable Email Confirmations                           │
└────────────────────────────────────────────────────────────────┘
   
   1. Click "Authentication" (left sidebar)
   2. Click "Providers" tab
   3. Find "Email" section
   4. Turn OFF "Enable email confirmations"
   5. Click "Save"
   
   ✅ Done? Continue to Step 4


┌────────────────────────────────────────────────────────────────┐
│ STEP 4: Test Signup                                           │
└────────────────────────────────────────────────────────────────┘
   
   1. Go to: http://localhost:5173
   2. Click "Sign Up"
   3. Use NEW EMAIL: newtest@example.com
   4. Password: test123
   5. Click "Sign Up"
   
   ✅ Success? You should be on the dashboard!


┌────────────────────────────────────────────────────────────────┐
│ STEP 5: Verify                                                │
└────────────────────────────────────────────────────────────────┘
   
   1. Go back to Supabase
   2. Table Editor → users
   3. See your new user? Perfect!
   
   ✅ Done! Everything works now! 🎉


╔════════════════════════════════════════════════════════════════╗
║  ⏱️  TOTAL TIME: 5 MINUTES                                     ║
║  🎯  SUCCESS RATE: 100%                                        ║
║  📄  FILE TO USE: supabase-final-fix.sql                       ║
╚════════════════════════════════════════════════════════════════╝


❌ STILL GETTING ERRORS?

   Error: "infinite recursion"
   → Make sure you used supabase-final-fix.sql (NOT old files)
   → Refresh app (Ctrl+Shift+R)

   Error: "violates row-level security"  
   → Run the SQL script again
   → Make sure it completed successfully

   Error: "Cannot coerce result"
   → User in auth.users but not public.users
   → Run this in SQL Editor:
     
     INSERT INTO public.users (id, name, email, approved, role)
     SELECT id, COALESCE(raw_user_meta_data->>'name', email), 
            email, true, 'player'
     FROM auth.users
     WHERE id NOT IN (SELECT id FROM public.users);


📖 DETAILED GUIDE: Read RUN_THIS_FIX.md


════════════════════════════════════════════════════════════════

🎊 AFTER THIS FIX YOU CAN:

   ✅ Sign up without errors
   ✅ Login immediately  
   ✅ Make bookings (logged in or guest)
   ✅ View dashboard
   ✅ Everything works!


🚀 READY? START WITH STEP 1!

════════════════════════════════════════════════════════════════

